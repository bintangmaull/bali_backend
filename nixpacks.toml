# nixpacks.toml for Flask + Rasterio/GDAL
[phases.setup]
    nixPkgs = ["python311", "python311Packages.pip", "gcc", "gdal", "expat", "proj", "geos", "pkg-config"]

[phases.install]
    cmds = ["pip3 install -r requirements.txt"]

[variables]
    GDAL_DATA = "/usr/share/gdal"
    PROJ_LIB = "/usr/share/proj"

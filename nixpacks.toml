# nixpacks.toml for Flask + Rasterio/GDAL
[phases.setup]
    nixPkgs = ["python311", "gcc", "gdal", "expat", "proj", "geos"]

[phases.install]
    cmds = ["pip install -r requirements.txt"]

[variables]
    GDAL_DATA = "/usr/share/gdal"
    PROJ_LIB = "/usr/share/proj"
